import Foundation
import SwiftUI

public enum Assets {
    // App Icon
    public static let appIcon = Image("AppIconImage")
    
    // Color assets
    public enum Colors {
        public static let accent = Color("AccentColor")
    }
}